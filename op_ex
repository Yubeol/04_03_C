//문제: 점수하나를 입력받아서 6의 배수인지 확인하는 코드 작성.
//다느 %6 사용하지 않는다

#include <stdio.h>

int main(void) {

	int num;

	printf("정수를 입력: ");
	scanf_s("%d", &num);

	if (num % 2 == 0 && num % 3 == 0) {
		printf("%d는 6의 배수입니다.\n", num);
	}
	else {
		printf("%d는 6의 배수가 아닙니다.\n", num);
	}
	return 0;
}
